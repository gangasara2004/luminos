# ── LUMINOS FRONTEND — copy this to .env for local dev ────────────────────────
# On Cloudflare Pages: add these in Settings > Environment Variables

# Your Railway backend URL — NO trailing slash
# Local dev:
VITE_BACKEND_URL=http://localhost:3001

# Production (set this in Cloudflare Pages dashboard, not here):
# VITE_BACKEND_URL=https://luminos-production-xxxx.up.railway.app
